# Sample Projects

These synthetic projects provide safe workflow examples for the sandbox. They
avoid workplace code and make it possible to demonstrate repeated Python
environment problems without exposing internal systems.

The initial fixtures cover:

- a passing Python project;
- a project with a missing dependency;
- a project with a functional test failure;
- a project that expects a missing environment variable.

